/**
 * One line per named technology: what it is, and what it replaced or why it won.
 *
 * Deliberately partial. The atlas carries 336 technology labels, but most are
 * generic nouns ("workflows", "documentation", "trade-offs") that a definition
 * would only pad. This covers the named, learnable things — protocols, tools,
 * systems, specific techniques — that a reader can look up and go use.
 *
 * Keys must match a `technologies` entry in capability-map.ts exactly; a test
 * enforces that so a renamed label cannot silently orphan its definition.
 */
export const TECHNOLOGY_GLOSSARY: Readonly<Record<string, string>> = {
  // Foundations
  "hash maps": "用一个函数把键直接算成存放位置，于是查找不用逐个比对——几乎所有「按标识找东西」的场景背后都是它。",
  "dynamic programming": "把大问题拆成会重复出现的小问题，算过的存下来不再重算；代价是你得先看出重叠在哪里。",
  complexity: "一把只看规模增长、不看机器快慢的尺子。它回答的是「数据翻十倍会怎样」，不是「这段代码要跑多少毫秒」。",
  Python: "语法接近伪代码、生态极大的语言，几乎是数据和 AI 领域的默认选择；代价是运行慢，且并发受全局锁限制。",
  TypeScript: "给 JavaScript 加上类型检查，让一部分错误在运行前就暴露；类型只在编译期存在，运行时它就是 JavaScript。",
  Go: "为服务端而生的语言：编译成单个可执行文件、并发写起来很轻，故意保持语言特性少，牺牲表达力换团队一致性。",
  Java: "运行在虚拟机上的老牌语言，生态和工具链极其成熟，是大型企业后端的长期默认项。",
  "async/await": "把「等待」写成看起来像顺序执行的语法，让等 IO 的代码不再靠层层回调；它不创造并行，只是让出等待的时间。",
  DNS: "把人记得住的域名翻译成机器用的地址。它有缓存，所以改了之后不会立刻生效——这是很多「只有我这边坏」的根因。",
  TCP: "在会丢包、会乱序的网络上，模拟出一条有序可靠的字节流。可靠是靠重传换来的，所以它的代价是延迟。",
  TLS: "给连接加密并验证对方身份，让中间人看不懂也改不了内容。握手要额外往返，这是首次连接慢的原因之一。",
  HTTP: "一次请求一次响应的文本协议，规定了方法、状态码和头部。整个 Web 的通用语，也是绝大多数接口的载体。",
  WebSocket: "在一条 HTTP 连接上升级成双向长连接，双方随时都能主动发消息；适合聊天和协作，代价是连接状态要自己管。",
  SSE: "服务器单向持续推送的轻量方案，基于普通 HTTP，自带断线重连；模型逐字输出的场景大多用它而不是 WebSocket。",
  gRPC: "用二进制格式和预先定义的接口描述做远程调用，比 JSON 更快更省，代价是不能直接在浏览器里调、也不便于人工查看。",
  threads: "操作系统调度的最小执行单位，共享同一份内存。共享带来了效率，也带来了必须加锁保护的那些问题。",
  locks: "保证同一时刻只有一个执行流能碰某份数据。它解决了竞争，同时引入了死锁和性能瓶颈这两个新问题。",
  "event loops": "单线程加一个待办队列，谁在等待就先放回队列去做别的；Node.js 和浏览器的并发模型，没有锁但也没有并行计算。",
  Linux: "服务器上事实上的操作系统。容器、权限、文件描述符、信号这些概念都来自它，理解它才看得懂线上现象。",
  Git: "分布式版本控制：每份克隆都是完整历史。它让分支变便宜，从而使并行开发和代码评审成为常态。",
  ADRs: "架构决策记录——一页纸写下当时的选项、选了哪个、因为什么约束。它的价值在半年后，而不是写的当天。",
  "static analysis": "不运行代码就找出可疑写法的工具。它挡住的是机械错误，从而让人工评审可以专心看抽象和边界。",

  // Product
  PRDs: "产品需求文档：把要做什么写成别人照着能做、做完能验收的说明。写不出验收标准，说明需求本身还没想清。",
  "acceptance criteria": "「怎样算完成」的具体条件。它把「用户可以管理设置」这类无法验收的句子，逼成可以逐条勾掉的清单。",
  RICE: "用触达人数、影响、信心、投入四个因子给需求打分排序。它的真实作用是逼你把假设写出来，而不是那个分数本身。",
  "A/B testing": "把用户随机分两组同时跑，只留一个变量不同，从而把「我做的」和「碰巧发生的」区分开。",
  "confidence intervals": "一个区间，表示真实值大概率落在哪里。它提醒你测量结果本身带着不确定性，不是一个精确的数。",
  "guardrail metrics": "为了防止主指标涨的时候别处在塌，专门盯住的那些指标。没有护栏的成功案例通常是从别处偷来的。",
  "feature flags": "用开关控制功能对谁可见，让部署和发布分开。它是灰度、A/B 和快速关闭故障功能的共同基础。",
  Figma: "浏览器里的协作设计工具，设计稿和交互原型可以直接给人试用；它让「先观察再开发」在成本上变得可行。",
  "usability testing": "让真人在你面前完成一个真实任务，你闭嘴看他卡在哪。三个人里两个卡在同一处就是结构问题。",
  "unit economics": "单个用户或单次调用带来多少收入、花掉多少成本。AI 产品尤其致命——推理成本会直接吃掉毛利。",
  TCO: "总拥有成本：除了买价，还包括迁移、培训、运维和退出的代价。比价只比第一项是选型最常见的错误。",
  "KPI trees": "把一个总目标层层拆成可以被具体团队影响的下级指标，从而让「谁该做什么」和「为什么」连得起来。",
  funnels: "把用户完成一件事的过程拆成有序几步，看每步流失多少。流失最大的那一步就是下一个该改的地方。",
  "event taxonomy": "统一约定埋点事件的命名和字段，让不同人算同一个指标能算出同一个数。没有它，数据永远在打架。",

  // Backend & data
  REST: "把接口组织成对「资源」的增删改查，用 HTTP 方法表达动作。约定简单、缓存友好，代价是复杂查询会很啰嗦。",
  OpenAPI: "用一份机器可读的文件描述接口契约，据此自动生成文档、客户端和校验。契约先行的落地形式。",
  GraphQL: "让调用方自己声明要哪些字段，一次请求取回所需的全部数据，解决了 REST 的多次往返和字段冗余；代价是服务端更难缓存和限流。",
  webhooks: "反过来由对方在事件发生时主动调你的接口，替代你不停轮询。你必须准备好接收重复和乱序的回调。",
  idempotency: "同一个操作执行多次和执行一次结果相同。因为网络重试不可避免，它是分布式系统里几乎所有写操作的前提。",
  "OAuth 2.0": "让用户授权第三方访问自己的数据，而不用把密码交出去。现代登录和 API 授权的基础框架。",
  OIDC: "在 OAuth 2.0 之上补齐「这个人是谁」的那一层，产出标准化的身份凭据；OAuth 管授权，它管认证。",
  JWT: "一段自带签名的凭据，服务端不查库就能验证真伪。代价是签发之后在有效期内无法收回，所以要配短有效期和刷新机制。",
  RBAC: "按角色授予权限，人归入角色、角色拥有权限。它简化了管理，但对「只能看自己那条数据」这类要求表达力不足。",
  OWASP: "一份被广泛引用的常见 Web 漏洞清单。它的价值是给自我攻击提供一份具体的、可逐条执行的路线。",
  PostgreSQL: "功能最全面的开源关系数据库，事务、约束、JSON、全文和向量检索都能扛；多数项目的合理默认选择。",
  indexes: "为常查的列另建一份有序结构，把全表扫描变成定位。代价是写入变慢、占额外空间——索引不是越多越好。",
  migrations: "把数据库结构的每次变更写成可重放、有顺序的脚本，让每个环境都能演进到同一状态且可追溯。",
  "query plans": "数据库为一条查询选择的执行方式。看它才知道索引有没有被用上——这是性能问题最直接的证据。",
  ACID: "事务的四个承诺：要么全做要么全不做、不破坏约束、并发下互不干扰、提交后不丢。关系数据库的核心卖点。",
  "isolation levels": "规定并发事务之间能看到对方多少中间状态。默认级别通常比人以为的弱，这是最贵的常见误解之一。",
  "optimistic locking": "不提前加锁，而是带上版本号提交，版本对不上就重试。适合冲突少的场景，避免了锁的等待成本。",
  outbox: "把「改数据」和「发消息」写进同一个事务，再由单独的进程读出来发送，解决了两者可能只成功一半的问题。",
  sagas: "跨服务的长流程没有事务可用，于是拆成一串带补偿动作的步骤：失败就反向执行已完成的部分。",
  Redis: "内存里的键值存储，快到可以当缓存、计数器、限流器和轻量队列用；代价是内存有限且默认不保证持久。",
  CDN: "把静态内容放到离用户更近的节点，缩短物理距离带来的延迟，同时替源站挡掉大量流量。",
  TTL: "给一份缓存或记录设一个存活时间，到点自动失效。最简单的失效策略，适合能容忍一定时间旧数据的场景。",
  "cache invalidation": "在原数据变化时主动让副本失效。缓存真正的难点——删早了浪费、删晚了出错、大家同时删会击穿数据库。",
  Kafka: "把消息当成可重放的日志而不是取走就没的队列，于是下游坏了可以从任意位置重跑；代价是运维复杂度高。",
  SQS: "云上的托管队列，简单可靠、按量付费，适合任务分发；不保证严格顺序，也不能像日志那样回放历史。",
  RabbitMQ: "成熟的消息中间件，路由规则灵活，适合复杂的分发拓扑；面向「消费即删除」而非长期保留。",
  Celery: "Python 生态里的后台任务框架，把函数调用变成排队执行，自带重试和定时；小项目最快的异步方案。",
  FastAPI: "Python 的现代 Web 框架，用类型注解同时完成参数校验和接口文档生成，契约先行的成本被降到很低。",
  "Node.js": "把 JavaScript 搬到服务端的运行时，单线程加事件循环，擅长高并发 IO，不擅长 CPU 密集计算。",
  Pydantic: "用 Python 类型声明数据结构并自动校验，把「相信输入」变成「验证输入」，是 AI 应用里结构化输出的常用落点。",
  pytest: "Python 的主流测试框架，写法接近普通函数，夹具机制让准备和清理可以复用。",
  "contract tests": "分别验证调用方的期待和提供方的实现是否符合同一份契约，从而在不跑整套系统的情况下防止接口漂移。",
  "property tests": "不写具体用例，而是描述「无论输入什么都必须成立的性质」，由框架生成大量随机输入去攻击它。",
  Spark: "把大数据的计算摊到很多机器上并行处理的框架，适合单机装不下的批量作业。",
  Airflow: "用代码定义有依赖关系的定时任务图，负责调度、重试和补跑历史；数据管道的常见骨架。",
  dbt: "在数据仓库里用 SQL 做转换，并给这些转换加上版本、测试和血缘。它让分析口径变成可评审的代码。",
  BigQuery: "按扫描量计费的托管数据仓库，不用管容量，适合分析型查询；代价是随手写的查询可能很贵。",

  // Product engineering
  DOM: "浏览器把页面解析成的一棵可被修改的对象树。所有前端框架最终都是在操作它。",
  React: "用「状态决定界面长什么样」的声明式写法替代手工改 DOM，由框架算出差异去更新。前端主流的心智模型。",
  "Next.js": "React 之上的全栈框架，把路由、服务端渲染和数据获取统一起来，让一部分工作可以移回服务器。",
  "Server Components": "在服务器上渲染、不把自身代码发到浏览器的组件，用来减少前端包体积并让数据获取更靠近数据源。",
  hooks: "让函数组件拥有状态和副作用的机制。用它做数据同步是 React 复杂度最常见的来源——派生状态应该现算而不是同步。",
  "React Query": "把服务端数据当缓存来管：有键、有新鲜度、有失效规则，顺带解决重复请求、竞态和后台刷新。",
  SWR: "同一类思路的轻量方案——先返回缓存再后台重新验证，让界面先出现再变准。",
  "optimistic UI": "先按预期更新界面再等服务器确认，失败了回滚。它让操作显得瞬时，代价是要处理「显示成功但其实失败」。",
  "design tokens": "把颜色、间距、字号等设计决定收敛成命名变量，让全局调整成为可能，也让硬编码值无处藏身。",
  WCAG: "国际通行的无障碍标准，规定了对比度、键盘可达、替代文本等要求；很多地区它同时是法律义务。",
  ARIA: "给元素补充语义信息，让读屏软件理解它的角色和状态。第一原则是能用语义化原生元素就不要用它。",
  "Core Web Vitals": "三个由真实用户数据定义的体验指标：主要内容多久出现、多久能响应、布局有没有乱跳。",
  "code splitting": "把前端代码按路由或功能拆成多个包，按需加载，缩短首屏要下载的体积。",
  virtualization: "长列表只渲染视口内的那几十行，其余用占位撑开高度，让上万条数据也能流畅滚动。",

  // Platform
  replication: "把同一份数据在多台机器上各存一份，防止单点丢失并分担读取；代价是副本之间存在同步延迟。",
  partitioning: "把数据按某个键切成多份分别存放，让单机装不下的数据可以水平扩展；分区键选错会造成热点。",
  consensus: "多个节点就「当前是什么状态」达成一致的协议（如 Raft）。它是分布式系统里所有强一致保证的基础。",
  AWS: "规模最大的云平台，服务最全、文档最多，也最容易在不知不觉中被绑定和产生意外账单。",
  GCP: "谷歌的云平台，数据分析和机器学习相关服务是它的强项。",
  VPC: "在云上划出的私有网络，决定谁能访问谁。安全的第一道边界，也是排查连不通问题的第一个怀疑对象。",
  IAM: "云上的身份和权限系统，规定哪个身份能对哪个资源做什么。最小权限原则的落地位置。",
  "object storage": "按对象存放文件的存储服务，容量近乎无限、单价极低，适合静态资源、备份和数据湖；不适合频繁小改。",
  Docker: "把程序和它的依赖打包成镜像，在哪跑都一样，终结了「在我机器上是好的」。",
  Kubernetes: "容器的调度和编排系统：决定容器跑在哪、挂了重启、按需扩缩。功能强大，学习成本也确实高。",
  Helm: "把一组 Kubernetes 配置打包成可参数化、可版本化的模板，避免同一套配置在各环境之间手工复制。",
  "GitHub Actions": "和代码仓库长在一起的流水线，事件触发、配置即代码，是中小团队最省事的持续集成方案。",
  Terraform: "用声明式代码描述云上资源，可以预览变更、可以销毁重建，让环境变成可复现的东西。",
  OpenTelemetry: "指标、日志和追踪的统一采集标准，让你换后端不用改埋点，避免被单一监控厂商绑定。",
  Prometheus: "拉取式的指标系统，自带查询语言和告警规则，是云原生监控的事实标准。",
  Grafana: "把各类数据源画成仪表盘的可视化工具，通常和 Prometheus 搭配使用。",
  "SLI/SLO": "SLI 是衡量服务质量的具体指标，SLO 是给它定的目标。有了目标才谈得上「允许失败多少」。",
  "error budgets": "由 SLO 反推出的允许失败额度。额度还有就大胆发布，额度用完就先修稳定性——它把可靠性变成可谈判的资源。",
  "circuit breakers": "下游连续失败时暂时切断调用并快速失败，避免自己被拖死，也给对方恢复的空间。",
  "chaos testing": "主动往生产或准生产环境注入故障，验证系统的容错设计是真的有效，而不只是写在文档里。",
  "load testing": "阶梯加压直到系统出现拐点，观察此时先饱和的是 CPU、IO、锁还是连接池。",
  "latency percentiles": "按分位数看延迟：p99 表示最慢的百分之一有多慢。平均值会把最差的那部分完全掩盖。",
  autoscaling: "按负载自动增减实例数量。要考虑冷启动时间——扩得太晚等于没扩。",
  "threat modeling": "在设计阶段系统性地问：谁会攻击、从哪进来、进来后能走多远。扫描器发现不了的设计缺陷靠它。",
  secrets: "密码、密钥和令牌。正确做法是集中托管、可轮换、按需下发，而不是写进代码或环境文件。",
  "SOC 2": "关于安全和流程控制的第三方审计认证，在美国 B2B 销售里常常是能不能进入采购流程的前提。",
  HIPAA: "美国医疗数据的隐私和安全法规，决定了医疗场景下数据能怎么存、怎么传、谁能访问。",
  "golden paths": "团队铺好的默认路径：照着走就自动带上监控、安全和部署配置。靠省事而不是靠规定来推动一致性。",

  // AI systems
  "supervised learning": "用大量「输入配正确答案」的例子训练模型，让它学会从输入预测答案。最主流也最依赖标注质量的范式。",
  embeddings: "把文字或图像映射成一串数字，使含义相近的东西在数值空间里也彼此靠近，从而可以按语义比较和检索。",
  PyTorch: "研究和生产都在用的深度学习框架，写起来像普通 Python，是当前事实上的默认选择。",
  transformers: "靠注意力机制并行处理整段序列的模型结构，取代了必须逐字推进的循环网络，是大模型时代的技术前提。",
  tokens: "模型看到的最小单位——文字被切成的片，不等于字或词。上下文长度和计费都按它算。",
  attention: "让模型在处理每一片时决定该「看」前文的哪些部分。它是 Transformer 能理解长距离关系的核心机制。",
  sampling: "从模型给出的下一片概率分布里挑一个。温度等参数控制随机程度，这也是同一输入两次结果不同的原因。",
  "context windows": "模型一次能看到的 token 上限。超出就必须裁剪或摘要，而相关信息一多，模型反而更容易被无关内容带偏。",
  "structured outputs": "强制模型按指定的结构输出，让下游程序可以直接解析，而不是用正则去猜它这次写成了什么样。",
  "few-shot": "在提示里放几个示范例子，让模型照着做。通常比反复描述规则更有效，代价是占上下文。",
  "prompt injection": "把指令藏在模型会读到的内容里（网页、文档、工具返回值）来劫持它的行为。提示层的约束挡不住它。",
  BM25: "经典的关键词检索打分算法。它精确匹配词，恰好补上语义检索最容易漏掉的那部分。",
  "vector search": "在嵌入空间里找离查询最近的若干个点，实现按含义检索；相近不等于正确，它会找回相关但没有答案的内容。",
  HNSW: "一种近似最近邻索引结构，用可控的精度损失换来数量级的检索加速，是多数向量库的默认实现。",
  "hybrid search": "把关键词检索和向量检索的结果融合排序，兼顾精确匹配和语义相近——实践中几乎总比单用一种好。",
  rerankers: "对初步检索出的候选再用更重的模型精排一遍。它是提升检索质量性价比最高的一步。",
  "vector databases": "为向量检索优化的存储，负责索引、过滤和扩展；数据量不大时用 PostgreSQL 的 pgvector 往往就够。",
  pgvector: "让 PostgreSQL 直接存储和检索向量的扩展，省掉引入一套独立向量库的运维成本。",
  "function calling": "模型按预先声明的格式输出一个「要调用哪个工具、参数是什么」的请求，由外部程序真正执行。",
  "JSON Schema": "描述数据结构的标准格式，用来约束模型的输出形状并在执行前校验参数。",
  MCP: "一套开放协议，让模型客户端以统一方式接入外部工具和数据源，避免每个集成都重写一遍。",
  LangGraph: "把 agent 显式建模成带状态和边的图，从而可以设置检查点、恢复中断和限制循环。",
  "state machines": "把流程写成有限的状态和明确的转移条件。给 agent 套上它，是把「模型自由发挥」变回可控的主要手段。",
  checkpoints: "在长流程的每一步之后保存状态，让中断可以从上次的位置继续，而不是从头再跑一遍。",
  "eval datasets": "一批带标准答案的样本，用来在每次改动后重新测量质量。覆盖典型、边界和已知失败三类。",
  "LLM-as-judge": "用模型给模型的输出打分，比人工便宜得多；但必须先验证它和人工判断一致，否则它可能在系统性地打错分。",
  tracing: "记录一次请求经过的每一段和各自耗时。在 AI 系统里它还要记录每一步的提示、工具调用和返回。",
  "regression gates": "把评估接进发布流程：分数低于阈值就不许上线。它是防止「悄悄变差」的唯一可靠手段。",
  guardrails: "在模型之外做的输入输出检查，用来挡住有害内容、越权操作和敏感信息外泄。",
  "red teaming": "主动用越狱、注入和诱导手法攻击自己的系统，把成功的攻击变成回归测试用例。",
  PII: "可以识别到个人的信息。它决定了哪些数据能记录、能发给第三方模型、能保留多久。",
  vLLM: "高吞吐的开源推理引擎，靠连续批处理和显存复用把 GPU 利用率拉高，是自建模型服务的常见选择。",
  "Hugging Face": "模型和数据集的公共仓库加配套库，是获取和试用开源模型最短的一条路径。",
  batching: "把多个请求合并成一批送进 GPU 提高吞吐。它同时会拉长单次延迟——吞吐和延迟在这里直接对冲。",
  quantization: "用更低精度存储模型权重，换取更小的显存占用和更快的推理，代价是一定的质量损失，必须实测。",
  GPU: "擅长大规模并行数值计算的硬件，模型训练和推理的实际瓶颈通常是它的显存而不是算力。",
  LangSmith: "面向 LLM 应用的调用追踪和评估平台，用来回答「这次为什么答错了」。",
  "Weights & Biases": "记录训练与实验过程的平台：超参、曲线、产物版本，让实验结果可比较、可复现。",
  "model registry": "模型版本的登记处，记录每个版本的来源、指标和状态，让上线和回滚有明确对象。",
  canary: "新版本先放给一小部分流量，对比核心指标没问题再逐步放大。出问题时爆炸半径被限制住。",
  ASR: "语音识别：把声音转成文字。识别错和理解错是两类完全不同的问题，要分开测量。",
  TTS: "语音合成：把文字读出来。实时对话场景里，它的首字延迟直接决定了交互是否自然。",
  WebRTC: "浏览器之间低延迟传输音视频的标准，实时语音交互的常见传输层。",
  CLIP: "把图像和文字映射到同一个嵌入空间的模型，于是可以用文字检索图片，也是多模态能力的基础之一。",
  "active learning": "优先标注那些模型最不确定的样本，用最少的标注量换最大的提升。",
  "synthetic data": "用模型生成训练或测试数据，补上真实数据稀缺的部分；风险是把模型自己的偏差喂回给自己。",
  SFT: "监督微调：用「输入配理想输出」的样本继续训练已有模型，主要用来固定格式、语气和领域习惯。",
  LoRA: "只训练少量新增参数而冻结原模型，用极小的算力和存储实现微调，还能按需切换多个适配器。",
  PEFT: "参数高效微调的统称，LoRA 是其中最常用的一种；核心思路都是「少改一点就够」。",
  RLHF: "用人类偏好训练一个奖励模型，再用强化学习调整模型行为。让模型「好好说话」的经典路径。",
  DPO: "直接用偏好数据优化模型，省掉单独训练奖励模型那一步，实现更简单且更稳定。",

  // Program delivery
  "critical path": "决定整体完工时间的那条最长依赖链。缓冲只该加在它上面，给非关键路径加缓冲是浪费。",
  "risk registers": "风险登记表：概率、影响、缓解措施、负责人、触发条件。只保留会改变行动的那些，其余删掉。",
  runbooks: "出事时照着做的操作手册。它的价值在于让不是原作者的人也能在半夜恢复服务。",
  "go/no-go": "发布前的正式判定：各职能逐一确认就绪与否，全部通过才发。它把发布从工程事件变成跨职能事件。",
  rollbacks: "退回上一个已知可用版本的能力。没有被真实演练过的回滚方案不算数。",
  SLAs: "对外承诺的服务水平和违约后果。它是合同条款，通常要比内部目标留出余量。",
  procurement: "采购流程：评估、比价、法务、审批。它常常比技术集成慢得多，是企业交付里最容易被低估的那一段。",
};
